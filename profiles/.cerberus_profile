# Cerberus Keyring Backend ("test", "file", "os" or "memory")
# Example: KEYRING_BACKEND="test"
# You can reimport your wallet with desired backend-type
# (`cerberusd keys add CerberusWalletName --recover --keyring-backend test`)
KEYRING_BACKEND="os"

# Password
# Required if keyring-backend is "file" or "os"
PASSWD=""

# Cerberus Wallet Name (`cerberusd keys list`)
# Example: DELEGATOR_NAME="EZStaking"
DELEGATOR_NAME=""

# Cerberus Wallet Address (`cerberusd keys show CerberusWalletName -a`)
# Example: DELEGATOR="cerberus16vcwhpzd6hfsm5n0fxz9880lvkgjnpwyuhmmnv"
DELEGATOR=""

# Cerberus Validator Address
# Example: VALIDATOR="cerberusvaloper16vcwhpzd6hfsm5n0fxz9880lvkgjnpwyxqncyt"
VALIDATOR="cerberusvaloper1sva8pqqcyehwmcrlye3ak0ujltfpp5jt58sjhl"

# Current Cerberus Chain ID
CHAIN_ID="cerberus-chain-1"

##########################################################
# In case you customized the Cerberus Node configuration,#
# you will want to edit this below parameters.           #
##########################################################

# The IP of your Cerberus Node
NODE_IP="127.0.0.1"

# The port of your Cerberus Node
NODE_PORT="26657"

# The home of your Cerberus Node
NODE_HOME="$HOME/.cerberus"

# The path to your Cerberus binary
# To get the full path to your cerberus binary you can use `whereis cerberusd` and replace the below value by.
# BINARY="$HOME/go/bin/cerberusd"
BINARY="/usr/local/bin/cerberusd"

####################
# MODIFY FOR TESTS #
####################

# Time to sleep in sec between two delegations
# Example: SLEEPING_TIME=$((5*60))
# Example: SLEEPING_TIME="30"
#SLEEPING_TIME=$((5*60))
SLEEPING_TIME=$(((10 + $RANDOM % 10) * 60))

DENOM="ucrbrus"
GAS_PRICES=""

##################
# DO NOT MODIFY! #
##################

# Depending on Cosmos SDK version
# - ACCOUNT     $binary query account
# - BANK        $binary query bank balances
BALANCES_FROM="BANK"

SDETAILS="--chain-id ${CHAIN_ID} --node tcp://${NODE_IP}:${NODE_PORT} --home ${NODE_HOME}"
DETAILS="${SDETAILS} --keyring-backend ${KEYRING_BACKEND}"
